# 00-orchestrator — Addendum: route to onboarding first, on a fresh install

One-line addition to `00-orchestrator/SKILL.md`'s "Running a full cycle"
step 1 (base file untouched). That step currently reads: *"If `MEMORY.md`
/ `USER.md` / the STAR bank don't exist yet or look thin, run
`07-context-architect` first."* — accurate, but as of this addendum
that's now the first move **inside** `onboarding/SKILL.md`'s Session 1,
not a bare call to `07-context-architect` on its own. Same trigger
condition, one extra hop: a thin/missing memory bank routes to
`onboarding`, which runs Phase 0-4 as before and then continues into the
rest of the settings catalog on its own paced schedule, rather than
`00-orchestrator` considering setup "done" the moment Phase 0-4 finishes.

No other change to this file — the pipeline-stage table, the daily-cap
enforcement, and "what this skill must never do" all stay exactly as
written.
